{
  "name": "cached-got",
  "version": "1.0.1",
  "description": "Cache the response from `got` to a file",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/piuccio/cached-got.git"
  },
  "scripts": {
    "lint": "eslint --ignore-path .gitignore --ignore-pattern flow-typed .",
    "test": "jest --coverage",
    "test:watch": "jest --watchAll",
    "posttest": "npm run lint"
  },
  "author": "Fabio Crisci",
  "license": "MIT",
  "devDependencies": {
    "eslint": "5.15.3",
    "got": "9.6.0",
    "jest": "24.5.0"
  },
  "peerDependencies": {
    "got": "9.x"
  },
  "engines": {
    "node": ">= 10"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
