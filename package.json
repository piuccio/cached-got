{
  "name": "cached-got",
  "version": "0.0.0",
  "description": "Cache the response from `got` to a file",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ignore-path .gitignore --ignore-pattern flow-typed .",
    "test": "jest --coverage",
    "test:watch": "jest --watchAll",
    "posttest": "npm run lint"
  },
  "author": "Fabio Crisci",
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "9.0.0",
    "eslint": "5.5.0",
    "got": "9.2.0",
    "jest": "23.5.0"
  },
  "peerDependencies": {
    "got": "9.x"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
